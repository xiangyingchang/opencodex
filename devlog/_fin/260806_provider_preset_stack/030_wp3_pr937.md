# 030 — WP3: rebase, verify, and merge PR #937 (Nscale + Vultr)

Head: `olddonkey:codex/572-nscale-vultr-model-apis` @ `d717f77e7` (single commit)
Base for rebase: `dev` AFTER WP2 (#872) lands — re-measure at execution time.

## What the PR changes

### `src/providers/registry.ts` — two entries, inserted after `hyperbolic`

- `nscale` — `https://inference.api.nscale.com/v1`, `openai-chat`, key auth,
  `defaultModel` and `models` both `["meta-llama/Llama-3.1-8B-Instruct"]`,
  `liveModels: true`, `preserveCustomDestination: true`,
  `parallelToolCalls: false`, `reasoningEfforts: []`, discovery bounded
  256 KiB / 256 rows filtered to that single id.
- `vultr` — `https://api.vultrinference.com/v1`, same shape, `defaultModel` and
  `models` both `["kimi-k2-instruct"]`, bounded 256 KiB / 256 rows filtered to
  that single id.

Both entries carry their primary-source citations as code comments, which is the
pattern the merged Volcengine work established. Keep those comments through the
rebase — they are the durable evidence record.

**Note a real discrepancy to check during the audit.** The free-directory row for
Nscale uses `https://inference.api.nscale.com/v1`, and the registry entry agrees.
But the PR body's evidence table cites `docs.nscale.com` chat/model/completion
pages generically. Confirm the registry base URL matches the host those docs
actually document before merging. Same for Vultr: the registry uses
`https://api.vultrinference.com/v1` while the free directory has no Vultr row —
verify against the cited Vultr Serverless Inference usage guide.

Each provider exposes exactly ONE model. That is intentional and evidence-bound:
Nscale's catalog mixes chat/image/embedding with no discriminator, so only the
model in its official tool-calling example is admitted; Vultr documents
`kimi-k2-instruct` as its sole tool-calling model. For a coding agent this is the
right call — a model that cannot complete a tool loop is worse than a missing
model.

### `src/providers/free-directory.ts`

Only the `nscale` row is enriched (`supportLevel`, `documentationUrl`,
`modelsUrl`, `lastVerified: "2026-08-03"`).

### Tests and fixtures

- `tests/nscale-vultr-provider.test.ts` (new, 259 lines)
- `tests/fixtures/nscale-models.json`, `tests/fixtures/vultr-models.json`
- `tests/provider-registry-parity.test.ts` — `EXPECTED_KEY_PROVIDER_IDS` += 2

## Conflict surface and resolution rules

> **Superseded by Amendment A3/A8 (`000_plan.md`).** The table below is a
> PREDICTION. Act on what the rebase actually reports.

| File | Conflict cause | Resolution |
| --- | --- | --- |
| `src/providers/registry.ts` | Insertion point is after `hyperbolic`, which is BEFORE the `commandcode` block WP1/WP2 grew — conflicts may be lighter than WP2's | Keep all six new rows across the three PRs. |
| `tests/provider-registry-parity.test.ts` | The array now carries four ids from WP1+WP2 | Union all six, in registry ORDER (deep-equal assertion). |
| `src/providers/free-directory.ts` | WP1 rewrote the `nebius` line directly above `nscale` | Keep both. |
| 10 locale docs | Rewritten twice already | **Regenerate from the measured count.** |

## Count regeneration (STRICT)

Expected after rebase: `76 { forward: 1, oauth: 8, key: 64, local: 3 }`. The PR
body predicts "75 / 64" from a stale base. Measure, then write.

## Verification

```
bun run typecheck
bun test --isolate tests/nscale-vultr-provider.test.ts tests/provider-registry-parity.test.ts tests/provider-model-discovery-contract.test.ts
bun run privacy:scan
git diff --check
```

Plus the 15-site count read-back (Amendment A7).

## Merge

Force-with-lease push, mark ready, merge into `dev`, confirm via `gh`. #572 stays
open — the umbrella covers more providers than this batch.
